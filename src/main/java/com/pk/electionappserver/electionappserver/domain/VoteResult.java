package com.pk.electionappserver.electionappserver.domain;

import javax.persistence.Column;
import javax.persistence.Entity;

@Entity(name = "VotesResults")
public class VoteResult {
    //TODO: ENCJA DO OMÓWIENIA
    //@Column(name = "user")
}
